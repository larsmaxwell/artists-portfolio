import { GetByTypePipe } from './get-by-type.pipe';

describe('GetByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GetByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
