import { GetImgUrlPipe } from './get-img-url.pipe';

describe('GetImgUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GetImgUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
