import { GetByCategoryPipe } from './get-by-category.pipe';

describe('GetByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new GetByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
